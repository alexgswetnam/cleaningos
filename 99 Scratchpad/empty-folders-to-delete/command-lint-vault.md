---
description: Structural health check — links, duplicates, classification, claims, conflict state
argument-hint: "[folder]"
allowed-tools: Read, Glob, Grep, Bash, Edit
---

Run the **`lint-vault` skill** — `.claude/skills/lint-vault/SKILL.md`. Follow it exactly.

Arguments: `$ARGUMENTS`

Path-scoped rules in `.claude/rules/` load automatically for the folders you touch.
Read `CONSTITUTION.md` when you need governance, evidence rules or the
contradiction protocol — not for every task.
